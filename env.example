# ============================================================================
# Plugg Gymnasie - Environment Variables
# ============================================================================

# ----------------------------------------------------------------------------
# CLIENT-SIDE (Safe to expose in browser)
# ----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://[your-project].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc... 
# ^ Supabase anon key is safe to expose (RLS protects data)

# ----------------------------------------------------------------------------
# SERVER-SIDE ONLY (NEVER ship to client)
# ----------------------------------------------------------------------------
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...
# ^ Admin key - use ONLY in Server Components, API Routes, Scripts
# HOW TO VERIFY IT'S NOT LEAKED:
#   1. Open browser DevTools → Console
#   2. Run: console.log(process.env.SUPABASE_SERVICE_ROLE_KEY)
#   3. Should print: undefined (if not, it leaked!)

# ----------------------------------------------------------------------------
# Email Notifications (for incident alerts)
# ----------------------------------------------------------------------------
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=your-email@gmail.com
EMAIL_SMTP_PASS=your-app-password
EMAIL_TO=your-email@gmail.com
# ^ Gmail setup:
#   1. Enable 2-factor authentication on Gmail
#   2. Generate "App Password" for this project
#   3. Use the app password (not your regular password)

# ----------------------------------------------------------------------------
# Optional: Backup Encryption Password
# ----------------------------------------------------------------------------
BACKUP_PASSWORD=your-strong-passphrase-20-chars-minimum
# ^ Used to encrypt database backups (choose strong passphrase)

# ----------------------------------------------------------------------------
# Optional: Vercel Analytics (cookieless, built-in)
# ----------------------------------------------------------------------------
# Auto-enabled in Vercel Dashboard → Analytics
# No environment variables needed
